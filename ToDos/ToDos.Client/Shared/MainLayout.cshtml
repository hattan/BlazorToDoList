@implements ILayoutComponent
    <div class='container-fluid'>

            <div class='col-sm-12'>
                @Body
            </div>

    </div>

    @functions {
    public RenderFragment Body { get; set; }
    }
